#ifndef GENERAL_H
#define GENERAL_H

#define  PIE   3.1415926536
#define  YYES  1
#define  NNO   0
#define INFINITE 1000000000

#define MIN(x,y)  ((x) < (y) ? (x) : (y))
#define MAX(x,y)  ((x) > (y) ? (x) : (y))

#endif
